node_modules/
package-lock.json
.env
.env.local
dist/
.next/
.turbo/
*.log
.pnpm-store/

# Uploaded user media (faces, ID cards, pickup proofs) — sensitive, not for VCS
apps/api/uploads/

# File giao việc nội bộ (không commit)
Quan_tasks.md
me_tasks.md

# Local Codex/agent guidance (không push)
AGENTS.md
apps/mobile/AGENTS.md
apps/mobile/UI_UX_TASTE_GUIDE.md

# Claude Code dev-server config — local only, không commit lên repo
.claude/launch.json

# Repo dùng pnpm. package-lock.json lọt vào là do lỡ chạy `npm install`, và nó khiến
# Vercel nhận nhầm package manager rồi chạy npm trên workspace pnpm.
package-lock.json
yarn.lock
