Provider->ScanScreen:scan receiver QR
    ScanScreen->ReservationsController:POST /reservations/scan (qrToken)
        ReservationsController->ReservationsService:scanQr(qrToken, scannerUserId)
            ReservationsService->PrismaService:reservation.findUnique(qrToken) include listing, receiver
                PrismaService->Database:SELECT reservations JOIN
            ReservationsService->PrismaService:providerProfile.findUnique(scannerUserId)
                PrismaService->Database:SELECT provider_profiles
            ReservationsService->PrismaService:reservation.update(status=picked_up, scannedBy)
                PrismaService->Database:UPDATE reservations
            ReservationsService->NotificationsService:notify(receiver, Pickup confirmed)
