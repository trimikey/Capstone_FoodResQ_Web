Shipper->ActiveTaskScreen:report failed (reason required)
    ActiveTaskScreen->DeliveriesController:POST /deliveries/:id/fail (reason)
        DeliveriesController->DeliveriesService:failDelivery(id, shipperUserId, reason)
            DeliveriesService->PrismaService:volunteerProfile.findUnique(userId)
                PrismaService->Database:SELECT volunteer_profiles
            DeliveriesService->PrismaService:delivery.findUnique(id)
                PrismaService->Database:SELECT deliveries
            DeliveriesService->PrismaService:delivery.update(status=failed, failedReason)
                PrismaService->Database:UPDATE deliveries
            DeliveriesService->PrismaService:volunteerProfile.update(isAvailable=true)
                PrismaService->Database:UPDATE volunteer_profiles
