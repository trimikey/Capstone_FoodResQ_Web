# ── FoodResQ API — Environment template ──
# Copy file này thành `.env` rồi điền giá trị thật. KHÔNG commit `.env`.

# DB CHUNG (Supabase). Lấy connection string thật từ trưởng nhóm / Supabase dashboard.
# Lưu ý: nếu password có ký tự đặc biệt (@ : / ?) phải URL-encode (vd '@' -> %40).
DATABASE_URL="postgresql://postgres:<PASSWORD>@db.qwariaudkacksbpnphoa.supabase.co:5432/postgres?sslmode=require"

JWT_SECRET="change-me"
JWT_REFRESH_SECRET="change-me"
REDIS_URL="redis://localhost:6379"
PORT=3001
ALLOWED_ORIGINS="http://localhost:3000"

# Google OAuth — Client ID từ Google Cloud Console (OAuth 2.0 Client, type: Web)
GOOGLE_CLIENT_ID=""
